filebeat:
  prospectors:
  -
      paths:
      - /var/log/syslog
    input_type: log
    document_type: syslog

  output:
  logstash:
    hosts: ["192.168.10.120:5044"]

shipper:
logging:
  files:
    rotateeverybytes: 10485760
